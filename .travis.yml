language: python

#which versions of python to test against
python:
  - "2.7"
  - "3.6"

# command to install dependencies
install:
  - pip install codecov
  - pip install coverage

script: 
  - python -m unittest discover -v ./ # command to run unit test
  - coverage run ./test.py # command to run code coverage
  
after_success:
 - codecov --token=c74dd386-73e2-4ba9-95d5-917823961198 # upload coverage report to codecov.io

deploy:
  provider: s3
  access_key_id: AKIAIINTJMOHJH3BM2WA
  secret_access_key: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
  bucket: https://cps847group19.s3.amazonaws.com/index.html
  local-dir: ./index.html
  region: us-east-1
